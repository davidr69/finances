package com.lavacro.finances.controllers;
/*
import org.junit.Test;
import org.junit.runner.RunWith;

import static org.assertj.core.api.Assertions.assertThat;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.ui.Model;

import com.lavacro.finances.util.ModelUtils;

@SpringBootTest
@RunWith(SpringJUnit4ClassRunner.class)*/
public class TestReports {
/*	@Autowired
	private Reports reportsController;

	@Test
	public void contextLoads() {
		assertThat(reportsController).isNotNull();
	}

	@Test
	public void test_balance_sheet() {
		Integer account = 6;
		Model model = new ModelUtils().makeModel();
		String rs = reportsController.balanceSheet(model, account);
		assertThat(rs).isEqualTo("balance_sheet");
		assertThat(model.getAttribute("account")).isEqualTo(6);
	}*/
}
